/result
/result-*
target/
*.rs.bk

# Publish artifacts (recorded image digest, oci: layout dry-runs); never committed.
/dist/

# E2E artifacts (logs, screenshots, terminal dumps, protocol traces)
/system-tests/artifacts/

.DS_Store
.direnv/

# `just dev-run`/`fresh-run` generated daemon launcher (LD_LIBRARY_PATH wrapper); never committed.
/.dev/

/user-stories/
*.code-workspace
.env

# Code-review / tech-debt tooling working data (never committed).
/.mrva/
codescene-hotspots*.json